const React = require('react');

var Counter = React.createClass({
    render: function() {
        return (
            <div>
                <h1>Counter</h1>
            </div>
        );
    }
});

module.exports = Counter;

const React = require('react');

//Components
// const Clock = require('Clock');

var Timer = React.createClass({
    render: function() {
        return (
            <div>
                <h1>Timer</h1>
            </div>
        );
    }
});

module.exports = Timer;
